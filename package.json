{
    "private": true,
    "scripts": {
        "lint": "bash -c 'prettier --write ${@:-.}' --"
    },
    "prettier": "@mpietrucha/prettier",
    "devDependencies": {
        "@mpietrucha/prettier": "^1.0.6"
    }
}
